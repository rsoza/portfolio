import { motion } from "framer-motion";

function ProjectDetailsPage() {
    return(
        <motion.div
        bg="var(--body_color)"
        >
            hello
        </motion.div>
    )
    
}

export default ProjectDetailsPage;